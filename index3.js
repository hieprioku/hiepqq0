let numbers = [3, 4, 6, -9, 10, -88, 2];
let user = Number(prompt("nhập text:"))

for (let i = 0; i < numbers.length; i++) {
    if (user == numbers[i]) {
        
    }
}
alert("user " + numbers[i] + "found at " + i);